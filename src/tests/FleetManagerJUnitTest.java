package tests;

// JUnit test removed: this project does not have JUnit on the classpath by default.
// The original JUnit-based tests were intentionally deleted to avoid build failures.
// If you want to add JUnit tests, add a build tool (Maven/Gradle) or place the
// JUnit jars on the compilation classpath, then restore proper test sources.
public class FleetManagerJUnitTest {

    public static void main(String[] args) {
        System.out.println("INFO: FleetManagerJUnitTest source removed to avoid JUnit dependency in src/.\n"
                + "Add JUnit via a build tool if you want to run unit tests.");
    }
}
